# This file should be encrypted with sops before committing.
# Run: nix-shell -p sops --run 'sops secrets/secrets.yaml'
#
# Plaintext structure (sops will encrypt the values):
mqtt_password_homeassistant: changeme
mqtt_password_zigbee2mqtt: changeme
borg_passphrase: changeme
