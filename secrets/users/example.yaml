# Example user secrets file for sops-menu (unencrypted reference).
# Copy this to <username>.yaml and encrypt with:
#   sops secrets/users/<username>.yaml
#
# Add a matching creation_rule to .sops.yaml for the new user.
# Point Home Manager at this encrypted file directly on hosts that should use
# sops-menu:
#   home-manager.users.<username> =
#     {
#       ...
#     }:
#     {
#       imports = [ ../../../home/<username>/headless.nix ];
#       user.cli.sops-menu = {
#         enable = true;
#         secretsFile = ../../../secrets/users/<username>.yaml;
#       };
#     };

entries:
  - name: "Example Service"
    username: "user@example.com"
    password: "changeme"
    url: "https://example.com"
    notes: "Example entry -- delete this"
    # otp: "JBSWY3DPEHPK3PXP"

  - name: "Another Service"
    username: "admin"
    password: "changeme"
    url: "https://another.example.com"
    notes: ""
